{
  "name": "cutcha-main",
  "version": "1.0.0",
  "main": "backend/dist/server.js",
  "private": true,
  "repository": {},
  "author": "HeleleF",
  "license": "MIT",
  "homepage": "",
  "engines": {
    "node": "14.15.5"
  },
  "dependencies": {
    "concurrently": "^6.0.0"
  },
  "scripts": {
    "build": "concurrently \"cd frontend && npm run build\" \"cd backend && npm run build\"",
    "heroku-postbuild": "npm run build",
    "install": "(cd frontend && npm install && npm list --depth=0) && (cd backend && npm install && npm list --depth=0)",
    "start:prod": "cd backend && npm run start:prod"
  }
}
